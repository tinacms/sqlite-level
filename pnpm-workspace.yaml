# pnpm 11 disables postinstall scripts of dependencies by default (strictDepBuilds).
# Allow only the deps that actually need their build step.
#   - better-sqlite3: native compilation via prebuild-install / node-gyp
#   - esbuild: downloads the platform binary (transitive via vitest)
allowBuilds:
  better-sqlite3: true
  esbuild: true
